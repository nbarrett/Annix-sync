name: Build and Push Docker Image

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: arc-runner-set
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Log in to Docker Registry
        uses: docker/login-action@v3
        with:
          registry: registry.tytodev.online
          username: tyto
          password: 42CgoXSCmvgBUK5G
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            registry.tytodev.online/${{ github.repository }}:latest
            registry.tytodev.online/${{ github.repository }}:${{ github.sha }}